// Nanni Groq-Key – hier deinen Key eintragen (von console.groq.com → API Keys).
// Diese Datei steht in .gitignore und wird NICHT ins Git hochgeladen (Sicherheit).
// Lokal: Key unten eintragen. Für GitHub Pages: nach dem Klonen nanni-config.example.js
// zu nanni-config.js kopieren und Key eintragen (oder Key nur lokal nutzen).

window.NANNI_GROQ_KEY = '';
